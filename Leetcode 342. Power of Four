class Solution {
    public boolean isPowerOfFour(int n) {
        if(n==1)
        return true;

        int i=1;
        while(Math.pow(4,i)<=n){
            if(Math.pow(4,i)==n)
            return true;

            i++;
        }

        return false;
    }
}
