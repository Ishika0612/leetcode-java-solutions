class Solution {
    public ListNode middleNode(ListNode head) {
    ListNode move=head; 
    int count=0;
    while(move!=null)   {
        count++;
        move=move.next;
    }
   for(int i=0;i<count/2;i++)
   head=head.next;
   return head;
    }
}
