class Solution {
    public int countDigits(int num) {
        int x=num;
        int digits=0;
        int y=0;
        while(num>0){
        y=num%10;
        if(x%y==0)
        digits++;
        num=num/10;
        }
        return digits;
    }
}
