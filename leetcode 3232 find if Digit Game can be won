class Solution {
    public boolean canAliceWin(int[] nums) {
        int single=0;
int doubl=0;
String s="";
for(int i=0;i<nums.length;i++){
s=Integer.toString(nums[i]);
if(s.length()==1)
single+= nums[i];
else
doubl += nums[i];
}

return single!=doubl;
    }
}
